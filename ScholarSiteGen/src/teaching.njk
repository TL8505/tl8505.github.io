---
layout: page.njk
title: "Teaching"
permalink: "/teaching/"
eleventyNavigation:
  key: Teaching
  order: 4
---

<div class="teaching-page">
  <div class="teaching-intro">
    <p>Here you can find information about my teaching experience and the courses I have been involved with.</p>
  </div>

  <div class="teaching-list">
    {% for course in collections.teaching %}
    <article class="teaching-item">
      <h2><a href="{{ course.url }}">{{ course.data.title }}</a></h2>
      <div class="course-meta">
        {% if course.data.course_code %}<span class="course-code">{{ course.data.course_code }}</span>{% endif %}
        {% if course.data.semester %}<span class="semester">{{ course.data.semester }}</span>{% endif %}
        {% if course.data.role %}<span class="role">{{ course.data.role }}</span>{% endif %}
      </div>
      <p class="course-description">{{ course.templateContent | excerpt(200) }}</p>
      <a href="{{ course.url }}" class="read-more">View details →</a>
    </article>
    {% endfor %}
  </div>
</div>